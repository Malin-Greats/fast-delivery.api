const { Paynow,Payment, StatusResponse, InitResponse } = require("paynow");
const paynow = new Paynow()
const payment = new Payment();// Payment(reference: any, authEmail: any, items: any): Payment
paynow.StatusResponse
  module.exports = {
    Paynow, Payment, StatusResponse, InitResponse
  };



